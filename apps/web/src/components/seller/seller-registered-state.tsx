import { SellerDashboardPage } from './seller-dashboard-page';

/**
 * Registered-seller workspace dashboard (presentation fixtures until the
 * Seller Gateway exists). Preview with `/seller?demo=registered`.
 */
export function SellerRegisteredState() {
  return <SellerDashboardPage />;
}

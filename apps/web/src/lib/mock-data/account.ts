/**
 * Legacy account fixtures are retired for live User Gateway data.
 * Navigation-only quick actions live on the account view-model.
 * Keep this module empty of fake customer PII.
 */

export {};
